# --- Core LLM / agents ---
openai>=1.40.0          # OpenRouter is OpenAI-API compatible
pydantic>=2.6.0
python-dotenv>=1.0.1
tenacity>=8.2.3

# --- HTTP ---
httpx>=0.27.0
requests>=2.31.0

# --- Data scraping ---
apify-client>=1.7.0     # optional, used when APIFY_TOKEN is set

# --- Numerics / prediction fallback ---
numpy>=1.26.0
pandas>=2.2.0

# --- CLI / UX ---
rich>=13.7.0
typer>=0.12.0

# --- Dashboard + backtester ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.3

# ---------------------------------------------------------------------------
# OPTIONAL: enable the Kronos deep-learning predictor.
#
# Step 1 - install torch + Hugging Face stack (sized for your hardware):
#     pip install torch>=2.2.0 transformers>=4.40.0 huggingface-hub>=0.23.0
#     # if you have a CUDA GPU follow https://pytorch.org/get-started/locally/
#
# Step 2 - clone the official Kronos repo and point KRONOS_REPO_PATH at it:
#     git clone https://github.com/NeoQuasar/Kronos.git
#     pip install -r Kronos/requirements.txt
#     export KRONOS_REPO_PATH=/abs/path/to/Kronos    # PowerShell: $env:KRONOS_REPO_PATH="C:\path"
#
# After that the dashboard's Backtest page will let you tick
# kronos-mini / kronos-small / kronos-base and run them side by side
# with the statistical baseline on real exchange data.
# ---------------------------------------------------------------------------
# torch>=2.2.0
# transformers>=4.40.0
# huggingface-hub>=0.23.0
# einops>=0.7.0           # required by the Kronos backbone
